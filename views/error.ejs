<%- include("./partials/header") %>

<div class="container tetx-center mt-5">
  <h1 class="text-muted">404 error</h1>
  <h1 class="text-musted">
    <% if (error && error.lengh > 0) {%>
      <%= error %>
    <%  } else { %> 
      <h2>The page you are looking for doesn't exist.</h2>
    <% } %>
  </h1>
  <a class="btn btn-danger btn-lg mt-5" href="/campgrounds">Go Home</a>
</div>

<%- include("./partials/footer") %>